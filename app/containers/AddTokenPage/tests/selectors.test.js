// import { selectAddTokenPageDomain } from '../selectors';

describe('selectAddTokenPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
