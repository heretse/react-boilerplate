/*
 * DevicesPage Messages
 *
 * This contains all the text for the DevicesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DevicesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DevicesPage container!',
  },
});
